/**
 * DEPRECATED — Tailwind v4 does NOT read this file.
 *
 * All design tokens (palette, shadows, animations, fonts, semantic
 * light/dark surface tokens) live in `src/app/globals.css` under `@theme`.
 * Editing this file has no effect on the build; it is kept only so that a
 * stale import or IDE plugin does not error out.
 *
 * → Add or change tokens in src/app/globals.css.
 */
import type { Config } from 'tailwindcss';

const config: Config = { content: [], theme: {}, plugins: [] };

export default config;
